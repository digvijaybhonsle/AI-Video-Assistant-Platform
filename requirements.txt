# =====================================================
# AI Video Assistant Platform — requirements.txt
# Optimized for Python 3.11.9 (Render.com)
# =====================================================

# ── Audio / Video Processing ────────────────────────────────
yt-dlp[default]>=2025.6.9
pydub>=0.25.1
ffmpeg-python>=0.2.0

# ── Speech-to-Text (Critical Section) ───────────────────────
openai-whisper>=20231117

# Pin Torch for Python 3.11 compatibility + faster install on Render
torch==2.4.1
torchaudio==2.4.1
# torchvision==0.19.1   # Uncomment only if needed

# ── Translation / Multilingual Support ─────────────────────
deep-translator>=1.11.4

# ── LangChain + LLMs ───────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-mistralai>=0.2.0
mistralai>=1.0.0

# ── Text Splitters ──────────────────────────────────────────
langchain-text-splitters>=0.3.0

# ── RAG / Vector Database ──────────────────────────────────
chromadb>=0.5.0
langchain-chroma>=0.1.0

sentence-transformers>=3.0.0
langchain-huggingface>=0.1.0
huggingface-hub>=0.23.0

tiktoken>=0.8.0

# ── Streamlit UI ───────────────────────────────────────────
streamlit>=1.38.0
streamlit-extras>=0.4.0
watchdog>=4.0.0

# ── PDF Export ─────────────────────────────────────────────
reportlab>=4.2.0
fpdf2>=2.7.9

# ── Environment & Utilities ────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.26.0
tqdm>=4.66.0
requests>=2.32.0
protobuf>=5.0.0
httpx>=0.27.0

# ── Build Dependencies (helps with Whisper/Torch) ───────────
setuptools>=70.0.0
wheel>=0.43.0